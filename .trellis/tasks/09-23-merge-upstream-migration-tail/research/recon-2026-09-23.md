# 侦察记录：2026-09-23

## 1. 仓库状态

| 项 | 值 |
|---|---|
| fork 分支 | `main` = `59d75050`（当前检出 `feat/credential-note`，与 `main` 同点） |
| 上游 | `upstream/main` = `f2840466`（`fix(grok): 把网关缓存键写回 Responses 请求体 (#737)`） |
| merge-base | `b211e7c8`（= 上一轮合并的上游基点） |
| 上游领先 | 33 个提交 / 342 个文件（+24104 / -1482） |
| fork 领先 | 21 个提交（donation、凭据备注、GHCR 流水线、Trellis 脚手架） |

## 2. 试合并冲突（`git merge-tree --write-tree main upstream/main`）

冲突 5 个文件，全部在 `internal/storage/`：

- `internal/storage/database_integration_test.go`
- `internal/storage/db_test.go`
- `internal/storage/migration.go`
- `internal/storage/migration_test.go`
- `internal/storage/operation_index_migration_test.go`

自动合并成功的关键文件：`internal/control/{http_routes.go,server.go,service.go,group_write.go}`、`internal/container/container.go`、`internal/parameteroverride/rules.go`、`web/src/frontends/modern/api/group-detail.ts`、`web/src/frontends/modern/i18n/locales/credential-cards.ts`。

## 3. 迁移编号冲突

上游本批新增（相对 merge-base）：`0018_auto_model`、`0019_auto_decision_attribution`（+测试）、`0020_client_model_overrides`、`0021_request_audit`。
fork 现行：`0018_donation_intake`、`0019_donation_manual_review`、`0020_credential_note`。

上游对这些编号的引用点（重命名时必须同步）：

```
internal/storage/migration.go:111-114            注册表 4 条
internal/storage/migration_test.go:32-35         wantIDs
internal/storage/db_test.go:680-683              wantMigrationIDs
internal/storage/database_integration_test.go:143  链式断言（含 0009–0017）
internal/storage/migrations/00{18,19,20,21}_*.go  ID 常量定义
```

## 4. 生产实测（yunyou-99，只读）

| 项 | 值 |
|---|---|
| 容器 | `gpt-load-2`，running / healthy，restarts 0 |
| 镜像 | `ghcr.io/0401lucky/gpt-load:sha-59d750501ada2957a87e545e9b1ef364a7af5164`（= fork main HEAD） |
| 账本 | **20 条**：`0001`–`0017` + `0018_donation_intake` + `0019_donation_manual_review` + `0020_credential_note` |
| 业务数据 | identities 1 / batches 36 / items 62 / resources 486 / review_actions 25 / test_attempts 33 |
| 凭据备注 | `credentials.note` 列存在（`0020` 已生效） |
| 数据库 | SQLite，卷 `gptload2-data`，`encryption.key` 与 db 同目录 |

结论：生产账本恰好等于 B 方案新注册表（§1.4）的**前 20 项前缀** ⇒ 新版启动后只追加 `0021`–`0024` 四行，**无需停机、无需任何 SQL 修正**。

> 访问方式（本机）：`ssh -F NUL -i C:/Users/lucky0401/.ssh/id_ed25519 -p 43477 root@64.83.25.99`（`~/.ssh/config` 在 Windows 下有权限告警，需绕开）。

## 5. 与上一轮的对比

| 维度 | 上一轮（2026-09-20） | 本轮 |
|---|---|---|
| 编号策略 | A：fork 让位上游，donation 顺延 0015/0016 → 0018/0019 | **B：fork 冻结 0018–0020，上游 4 条顺延为 0021–0024** |
| 生产升级 | 停机 + 整卷备份 + 删账本 2 行 + 重放 5 条迁移 | 纯追加 4 行，`pull` + `up -d` |
| 上游编号处理 | 保持原样 | 重命名文件 + 导出符号（schema 级内部名保持原样） |

## 6. 待确认项

- 上游 4 条迁移在 fork 链尾（晚于 donation/credential）执行的顺序敏感性：四条均为加列/建表型，读代码未见跨迁移依赖，但仍需用全量测试 + 生产副本实测确认（`design.md` §4 风险 1）。
- 上游 `web/` 改动 120 个文件，fork 亦有少量前端改动，构建结果需实测。
